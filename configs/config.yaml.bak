system:
  log:
    level: <value>
    path: <value>

host:
  spBlogAPIHost: <value>

database:
  mysql:
    master:
      host: <value>
      port: <value>
      username: <value>
      password: <value>
      dbName: <value>
      conn:
        maxIdle: <value>
        maxOpen: <value>
        maxLifetime: <value>

gin:
  # mode: debug、release、test
  mode: <value>

gorm:
  log:
    # logMode: silent、error、warn、info, default silent
    mode: <value>

validator:
  # 設定錯誤訊息文字語言
  locale: <value>
