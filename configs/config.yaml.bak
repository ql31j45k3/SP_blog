system:
  log:
    level: <value>
    path: <value>

host:
  spBlogApisHost: <value>

database:
  host: <value>
  port: <value>
  username: <value>
  password: <value>
  dbname: <value>

gin:
  # mode: debug、release、test
  mode: <value>

gorm:
  log:
    # logMode: silent、error、warn、info, default silent
    mode: <value>

validator:
  # 設定錯誤訊息文字語言
  locale: <value>
